Creating a virtual Env:

python -m venv venv

Activating a virtual Env:

.\venv\Scripts\activate

--------------------------------------

How to push code:

git add .
git commit -m "..."
git push

--------------------------------------

CHecking changes I made:

git status